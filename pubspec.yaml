name: ofgconnects_mobile
description: "A Christian Social app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- 1. BACKEND & NETWORK ---
  appwrite: ^14.0.0
  minio: ^3.5.8
  flutter_dotenv: ^5.1.0

  # --- 2. STATE MANAGEMENT ---
  flutter_riverpod: ^2.6.1
  state_notifier: ^1.0.0

  # --- 3. NAVIGATION ---
  go_router: ^14.0.0

  # --- 4. MEDIA & UPLOAD ---
  image_picker: ^1.1.1
  image_cropper: ^8.0.2
  flutter_cache_manager: ^3.3.2
  path: ^1.9.0

  # --- 5. VIDEO PLAYER ---
  video_player: ^2.8.6
  chewie: ^1.8.1
  video_thumbnail: ^0.5.3  # <--- ADD THIS
  path_provider: ^2.1.2    # <--- ADD THIS
  
  # --- 6. UTILITIES ---
  intl: ^0.19.0
  share_plus: ^10.0.0
  cupertino_icons: ^1.0.6

  # --- 7. MONETIZATION (ADDED) ---
  google_mobile_ads: ^5.1.0
  cached_network_image: ^3.4.1

dependency_overrides:
  flutter_web_auth_2: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/logo.png  # <--- ADD THIS LINE
    - assets/logoH.png